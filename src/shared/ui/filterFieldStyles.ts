import styles from "./ListFilters.module.css";

export const filterFieldClassName = styles.field;
export const filterFieldWideClassName = `${styles.field} ${styles.fieldWide}`;
export const filterFieldServiceClassName = `${styles.field} ${styles.fieldService}`;
